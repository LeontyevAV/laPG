# laPG

## Архитектура

- Главный модуль: `main.py` (меню 7 пунктов)
- Функциональные модули: `backup.py`, `restore.py`, `db_utils.py`, `settings.py`
- `backup.py` и `restore.py` можно запускать отдельно (с CLI-флагами)
- Взаимодействие с PostgreSQL через CLI-утилиты (`pg_dump`, `pg_restore`, `psql`, `createdb`, `dropdb`) — `subprocess`
- `asyncpg` используется только для получения списка БД с сервера

## Команды

```bash
# Главное меню
python main.py

# Бекап (интерактивный выбор БД)
python backup.py
# Бекап всех БД параллельно
python backup.py --all

# Восстановление
python restore.py
```

## Конфигурация

**`.env`** — только секреты:
```
DB_PASSWORD=your_password
```

**`settings.yaml`** — все остальные настройки:
```yaml
connection:
  host: localhost
  user: postgres

backup:
  default_compress: 0
  default_keep: 0
  databases:
    labots:
      compress: 9
      keep: 20

restore:
  backup_dirs:
    - backup
    - restory

scheduler:
  enabled: false
  cron: "0 2 * * *"
```

`.env` имеет приоритет над `settings.yaml` (DB_HOST/USER из `.env` переопределяют `connection`).

## Структура

- `main.py` — главное меню (8 пунктов)
- `backup.py` — создание дампов (интерактивный и `--all` режимы)
- `restore.py` — восстановление из дампа
- `cron.py` — управление cron (чтение/запись crontab, перезапуск)
- `db_utils.py` — общий модуль: получение списка БД, ротация бекапов
- `settings.py` — загрузка и модель конфигурации (pydantic + YAML)
- `settings.yaml` — конфигурация (пути, ID, флаги, списки)
- `.env` — секреты (пароль)
- `backup/`, `restore/` — директории с `.dump`-файлами

## Соглашения

- Дампы в формате custom (`-F c`) с именем `{db}_backup_{timestamp}.dump`
- Список БД получаем через `asyncpg` с fallback `postgres` → `template1`
- `--keep N` — удаляет старые дампы, оставляя N последних (по дате в имени файла)
- Восстановление: drop + create + pg_restore
- `backup.py`/`restore.py` импортируются из `main.py` без исполнения (guarded by `if __name__`)
- Настройки: `.env` (secrets) + `settings.yaml` (всё остальное), мерж через `settings.py`

## TODO

### В разработке
- cron: доделать `--install-cron` CLI-флаг для `main.py`
- alias: добавить пункт в меню или `--install-alias` для `~/.bashrc`

### Roadmap

#### 1. Бекапы и restore (наша база)
- ✅ Создание бекапа (одна БД / все)
- ✅ Восстановление (в существующую / новую)
- ✅ Ротация (keep N)
- ✅ Сжатие (-Z)
- ✅ Cron-расписание
- ❌ Верификация бекапов — проверка целостности дампа после создания
- ❌ Резервное копирование по расписанию с разными политиками (full/differential)

#### 2. Хранение
- ❌ S3 / Object Storage — пушить дампы в облако
- ❌ Yandex Disk (rclone + WebDAV) — пушить дампы на Яндекс.Диск
- ❌ SFTP/SCP — отправлять на другой сервер
- ❌ Шифрование — gpg перед отправкой
- ❌ Статистика по бекапам — размер, занятое место, сколько всего хранится

#### 3. Мониторинг и оповещения
- ❌ Telegram / Email / Webhook — уведомления об успехе/ошибке
- ❌ Проверка здоровья БД — доступность, размер, количество подключений
- ❌ Мониторинг диска — если мало места — предупредить до бекапа

#### 4. Управление серверами
- ❌ Несколько подключений — сохранённые профили (хост, порт, пользователь)
- ❌ История операций — audit log: кто, когда, что сделал
- ❌ Просмотр БД — список, размер, владелец, кодировка

#### 5. Интерфейс
- ❌ Web UI — FastAPI + Jinja2 / HTMX
  - Дашборд: состояние БД, последние бекапы, свободное место
  - Кнопки: бекап, восстановить, удалить
  - Лог операций
  - Настройки через браузер (settings.yaml)

#### 6. Сравнение БД
- ❌ Выбор двух БД из списка (`fetch_databases`)
- ❌ Сравнение схемы: таблицы, колонки (тип, default, nullable)
- ❌ Сравнение схемы: индексы, constraints, функции, триггеры, вьюхи
- ❌ Сравнение данных: количество строк по таблицам
- ❌ Построчный data diff (для небольших таблиц)
- ❌ Генерация SQL-миграции (через `migra` или свой генератор)
- ❌ Пункт в меню `main.py`
